# Changelog

## Unreleased

- Repository laid out on the package skeleton: bare `LICENSE` → `LICENSES/Apache-2.0.txt` + `NOTICE` + `REUSE.toml`; `src/loomground_workspace/_version.py` as the version source; `main.yml` CI and release-please added. No code changes.
